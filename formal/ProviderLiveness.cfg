\* Add statements after this line.
SPECIFICATION Spec

\* Bound for finite model checking. The DB can change flags this
\* many times. 3 is enough to exercise all protocol paths.
CONSTANT MaxDbVersion = 3

\* --- Liveness properties ---
\* TLC checks these over every possible infinite behavior.
\* If any behavior violates a property, TLC prints a counterexample
\* trace showing exactly which interleaving broke it.

PROPERTY ShutdownTermination
PROPERTY CacheFreshness
PROPERTY ReconnectLiveness
PROPERTY StaleOnDisconnect
